#include <bits/stdc++.h>

typedef long long ll;
typedef unsigned long long ull;

using namespace std;

#define debug(x) cout << #x << ": " << (x) << endl
#define debugp(p) cout << p.first << " " << p.second << endl
#define debugc(c) for(auto &i: c){cout << (i) << " ";} cout << endl

void solve(){

    

}

int main(){
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);

    int T = 1;
    cin >> T;
    while(T--) solve();
}
